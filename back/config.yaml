# 服务器配置
server:
  port: "8080"  # 后端端口
# MySQL配置（替换为你的实际信息）
mysql:
  host: "localhost"
  port: "3306"
  dbname: "my_blog"
  username: "root"
  password: "123456"  # 你的MySQL密码
  charset: "utf8mb4"
# JWT配置
jwt:
  secret: "my-blog-2025-jwt-secret"  # 自定义密钥（生产环境换复杂值）
  expire: 7200  # 令牌过期时间（秒），2小时